console.log("welcome to anubhav trainings");
var reuse = module.require('./reuse.js');
    reuse.printMyName();
    console.log(reuse.addNumbers(6,9));
    console.log(reuse.manipulate(7,7,'*'));
    console.log(reuse.calculateSize(["apple","banana","cherry"]));